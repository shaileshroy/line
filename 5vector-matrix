Write a program to do the following- 
i)	Find the vector-matrix multiplication of a r by c matrix M with an c- vector u. 
ii)	Find the matrix- matrix product of M with a c by p matrix N. 
import numpy as np

# Define the matrices and vectors
M = np.array([[1, 2, 3], [4, 5, 6]])  # Define a 2x3 matrix
u = np.array([2, 1, 3])              # Define a 1D vector
N = np.array([[7, 8], [9, 10], [11, 12]])  # Define a 3x2 matrix

# i) Vector-Matrix Multiplication (u * M)
result_vector_matrix = np.dot(u, M)
print("Result of Vector-Matrix Multiplication (u * M):")
print(result_vector_matrix)

# ii) Matrix-Matrix Multiplication (M * N)
result_matrix_matrix = np.dot(M, N)
print("\nResult of Matrix-Matrix Multiplication (M * N):")
print(result_matrix_matrix)

